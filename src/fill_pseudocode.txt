generate pixel map

fill = false
pixel_count = 0
num_lines = 0

for row in map
    for col in map
        if pixel is not white
            fill = true
            num_lines = 1
        if fill == true
            change pixel color to blue